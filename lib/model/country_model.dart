class MaxCountry {
  final String? name, dialCode, code, flagEmoji, flagIconCircle, flagIconSquare, nameAr;

  MaxCountry(
      {this.name,
      this.dialCode,
      this.code,
      this.flagEmoji,
        this.nameAr,
      this.flagIconCircle,
      this.flagIconSquare});
}
